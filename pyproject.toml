[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "daaq"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "uxarray>=2025.05.2",
]

[tool.setuptools.packages.find]
where = ["src"]
